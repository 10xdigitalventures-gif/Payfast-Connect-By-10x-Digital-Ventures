import {query} from './db';import {getValidSwichGhlToken} from './swich-ghl-token';const ENDPOINT='https://backend.leadconnectorhq.com/payments/custom-provider/webhook';
export async function sendSwichGhlNotification(input:{locationId:string;ghlTransactionId:string;chargeId:string;amount:number;contactId?:string|null;invoiceId?:string|null;orderId?:string|null}){const rows=await query<any[]>('SELECT provider_api_key FROM swich_ghl_installations WHERE location_id=? LIMIT 1',[input.locationId]);const apiKey=String(rows[0]?.provider_api_key||'');if(!apiKey)return{ok:false,status:400,error:'Swich provider key missing'};const token=await getValidSwichGhlToken(input.locationId);if(!token)return{ok:false,status:401,error:'Swich GHL token unavailable'};const now=Math.floor(Date.now()/1000);const payload:Record<string,unknown>={event:'payment.captured',chargeId:input.chargeId,ghlTransactionId:input.ghlTransactionId,locationId:input.locationId,apiKey,chargeSnapshot:{id:input.chargeId,status:'succeeded',amount:Number(input.amount),chargeId:input.chargeId,chargedAt:now}};if(process.env.SWICH_GHL_MARKETPLACE_APP_ID)payload.marketplaceAppId=process.env.SWICH_GHL_MARKETPLACE_APP_ID;if(input.contactId)payload.contactId=input.contactId;if(input.invoiceId)payload.invoiceId=input.invoiceId;if(input.orderId)payload.orderId=input.orderId;const response=await fetch(ENDPOINT,{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${token}`},body:JSON.stringify(payload)});const text=await response.text();return response.ok?{ok:true,status:response.status}:{ok:false,status:response.status,error:text};}
